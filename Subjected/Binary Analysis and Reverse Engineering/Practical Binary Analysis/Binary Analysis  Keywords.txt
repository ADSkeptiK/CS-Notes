gcc -E -P : Stop after preprocessing,avoid debugging.13

gcc -S  filename.c : Tells the compiler to to compile the code to x86 Assembly.13

gcc -S  -masm=intel: tells gcc to turn the format to intel and masm (from the default AT&T).14

cat file.extension: Used to open file.15

gcc -c filename.c: Used to create obj file of
the code.156

file file.fileextension: Used to get info abt file.16

LSB: Means it's small endian and stands for Least Significant Byte (Not to be confused with occurances where the LSB stands for Least Significant Bit!).16


ELF: Is a file format in Linux that is used for
saving the compiled object files, the
executables,shared libraries.16 

Relocation symbols: Specify how function and variable references should be resolved.17

readelf filename.extension : Loads the file as ELF.18

readelf -a/readelf –all: Used to load an entire elf file and get info abt it.SELF LEARNED

readelf --syms filename.extension: Reads the files symbols section.18

strip --strip-all filename.extension: Strips the executable from debug info.20


objdump -sj .rodata filename.elf:
Basically disassembles the text section of the file.21

objdump -s filename.elf: Displaying the bytes in each section in hex editor ish style.SELF LEARNED

objdump -sj sectionName filename.elf/
objdump -s -j sectionName filename.elf:
Used to display a specific section of binary.21

Objdump -d: Actually disassembles the file.21

.rodata: Means read only data.21


