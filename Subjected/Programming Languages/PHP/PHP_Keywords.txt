<?php coode ?>: Used to write php code we better not close the tag when it's only PHP code in the source file.

<?=:Another way of writing php tag.

<? ?>:Another way of writing php tag.

echo "string"; : prints the string and recognizes and reads html/css tags as in their own terms. Also $vars will be written as their value while in " " but in ' ' they will be literals.
  
  .: Is used for concatination in echos arguments(The string
  arguments have to be between single quotes).

$: starts the name of a variable.

const : creates constant.

define("CONSTANTNAME","CONSTANTVALUE",true/false):
If the third argument is true the CONSTANTNAME will become case INsensitive.

vardump($var): Gives you the type(value) of the var and is also used for printing arrays as it cannot be done with echo


for( looprange-array name/array as datamemeber/arraymember){}: Used as range based loop.



continue: has the same meaning as in c

array(): used to initialize a variable as array.
[]:Another newer way to initialize the array.